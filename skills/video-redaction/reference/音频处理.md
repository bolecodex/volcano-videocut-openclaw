# 音频处理

流程

1.  提取用户输入的文件地址
2.  执行脚本 `scripts/av_separation.py` 进行音视频分离、人声背景音分离工作
3.  提取人声文件地址 `VOCALS_MP3` 调用脚本 `scripts/audio_base64.py` 进行人声音频 base64 转换
4.  使用音频 base64 调用脚本 `scripts/asr_qwen.py` 进行 `asr` 提取
5.  输出 asr 识别结果
